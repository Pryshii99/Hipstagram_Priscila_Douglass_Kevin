node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQube Analysis') {
    def scannerHome = tool 'SonarScanner';
    withSonarQubeEnv() {
      sh "${scannerHome}/bin/sonar-scanner"
    }
  }
}
// hacete el commit de los dos archivos nuevos okey
// raro no me aparecen esos dos archivcos cuando hago el git status